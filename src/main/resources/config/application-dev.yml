# ===================================================================
# Spring Boot configuration for the "dev" profile.
# This configuration overrides the application.yml file.
# ===================================================================

logging:
  level:
    ROOT: DEBUG
    tech.jhipster: DEBUG
    org.hibernate.SQL: DEBUG
    edu.goatbot.gateway: DEBUG

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://admin:${jhipster.registry.password}@localhost:8761/eureka/

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false

  jackson:
    serialization:
      indent-output: true

  cloud:
    config:
      uri: http://admin:${jhipster.registry.password}@localhost:8761/config
      name: goatBotGateway
      profile: dev
      label: main

  # ============================================================
  # Base de datos SQL Server (R2DBC + JDBC)
  # ============================================================

  r2dbc:
    url: r2dbc:sqlserver://localhost:1433/goatboot?trustServerCertificate=true&encrypt=false
    username: sa
    password: D123s456

  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=goatboot;encrypt=true;trustServerCertificate=true
    username: sa
    password: D123s456
    hikari:
      pool-name: Hikari
      auto-commit: false
      maximum-pool-size: 5

  #jpa:
  #database-platform: org.hibernate.dialect.SQLServerDialect
  #show-sql: true
  #hibernate:
  #ddl-auto: none
  #properties:
  #hibernate:
  #format_sql: true
  #use_sql_comments: true

  liquibase:
    enabled: true
    contexts: dev, faker
    url: jdbc:sqlserver://localhost:1433;databaseName=goatboot;encrypt=true;trustServerCertificate=true
    user: sa
    password: D123s456

  mail:
    host: localhost
    port: 25
    username:
    password:

  messages:
    cache-duration: PT1S

  thymeleaf:
    cache: false

server:
  port: 8080
  forward-headers-strategy: native

# ===================================================================
# JHipster specific properties
# ===================================================================

jhipster:
  gateway:
    rate-limiting:
      enabled: false
      limit: 100000
      duration-in-seconds: 3600
  registry:
    password: admin
  cors:
    allowed-origins: 'http://localhost:8100,https://localhost:8100,http://localhost:9000,https://localhost:9000,http://localhost:9060,https://localhost:9060,http://localhost:9061,https://localhost:9061,http://localhost:9062,https://localhost:9062,http://localhost:9063,https://localhost:9063,http://localhost:9064,https://localhost:9064,http://localhost:9065,https://localhost:9065,http://localhost:9066,https://localhost:9066,http://localhost:9067,https://localhost:9067,http://localhost:9068,https://localhost:9068,http://localhost:9069,https://localhost:9069'
    allowed-origin-patterns: 'https://*.githubpreview.dev'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${jhipster.clientApp.name}-alert,X-${jhipster.clientApp.name}-error,X-${jhipster.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800
  security:
    authentication:
      jwt:
        base64-secret: NjhhZjg2NzgxNWE4MTllYmIwNjYzNjQ4MDc2NGZmMGNmMTFlYWFhOGRjNmQ3ZGU5ZTAyNDNlODY1NGQyOGQ4NGQ2ZTk1MzcyYTE5MmIxYWJmYWFlMmEzNTE2YTliNmI1NTQwMDU3M2RkMWIyNTc5M2ZjODM3MzAyMzVjYjkyYTM=
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  mail:
    base-url: http://127.0.0.1:8080
  logging:
    use-json-format: false
    logstash:
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# ===================================================================

# application:
